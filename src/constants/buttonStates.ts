export enum ModalState {
  NotConfirmed,
  InProgress,
  Successful
}

export enum ButtonState {
  Default,
  EnterAmount,
  Approving,
  Approved,
  Next,
  Confirming,
  InsufficientBalance,
  Retry,
  MaxCap,
  Swap
}
