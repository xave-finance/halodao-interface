export enum ModalState {
  NotConfirmed,
  InProgress,
  Successful
}

export enum ButtonState {
  Default,
  EnterAmount,
  Approving,
  Approved,
  Next,
  Confirming,
  InsufficientBalance,
  Retry,
  MaxCap,
  Swap,
  InsufficientLiquidity
}

export enum SwapButtonState {
  Default,
  EnterAmount,
  Approving,
  Approved,
  Next,
  Confirming,
  Retry,
  MaxCap,
  Swap,
  InsufficientLiquidity,
  InsufficientBalance
}
